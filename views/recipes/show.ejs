<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Recipes</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Welcome to <%= recipe.name %> Recipe</h1>
    <ul>
        <% recipe.ingredients.forEach((ingredient) => { %>
         <li>
            <a href="/ingredients/<% ingredient._id %>">
                <%= ingredient.name %>
                <form action="/ingredients/<%= ingredient._id %>?_method=DELETE" method="post">
                    <button type="submit">Delete Ingredient</button>
                </form>
            </a>
         </li>
        <% }) %>
    </ul>
    <a href="/recipes/<%= recipe._id %>/edit">Edit Recipe</a>
    
</body>
</html>